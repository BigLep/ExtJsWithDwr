<!DOCTYPE dwr PUBLIC
	"-//GetAhead Limited//DTD Direct Web Remoting 2.0//EN"
	"http://getahead.org/dwr/dwr20.dtd">

<dwr>
	<allow>
		<!-- 
			See web.xml for the classes DWR will generate interfaces for.
			We don't declare them and their converters here because we use DWR's annotation mechanism.
		-->
	</allow>
</dwr>

